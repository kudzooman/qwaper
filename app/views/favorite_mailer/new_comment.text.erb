New comment on "<%= @post.body %>"
<%= "=" * (@post.body.length + 12) %>

By: <%= @comment.user.name %>

<%= @comment.body %>

Visit online at: <%= post_comment_url(@comment.post, @comment, anchor: "comment-#{@comment.id}") %>