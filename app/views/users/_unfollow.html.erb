<%= form_for(current_user.relationships.find_by(followed_id: user.id),
             html: { method: :delete }) do |f| %>
  <div class="unfollow"><%= f.submit "Unfollow", class: "basic ui button" %></div>
<% end %>